{
  "compilerOptions": {
    "baseUrl": ".", // Ensures paths are relative to the project root
    "paths": {
      "@models/*": ["models/*"],     // Maps `@models` to the `models` directory
      "@components/*": ["components/*"], // If you have a `components` folder
      "@utils/*": ["utils/*"]         // For utilities or other common directories
    }
  }
}
